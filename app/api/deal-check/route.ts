import { NextRequest,NextResponse } from "next/server";
function outputText(payload:any){if(typeof payload?.output_text==="string")return payload.output_text;const out:string[]=[];for(const item of payload?.output??[])for(const part of item?.content??[])if(part?.type==="output_text"&&typeof part.text==="string")out.push(part.text);return out.join("");}
export async function POST(request:NextRequest){try{const body=await request.json();const image=String(body?.image||"");if(!image.startsWith("data:image/"))return NextResponse.json({error:"Upload a screenshot or photo of the deal."},{status:400});if(image.length>14_000_000)return NextResponse.json({error:"Image is too large."},{status:413});const key=process.env.OPENAI_API_KEY;if(!key)return NextResponse.json({error:"Deal Check AI is not configured yet."},{status:503});const response=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({model:process.env.OPENAI_MODEL||"gpt-5.6-luna",store:false,reasoning:{effort:"low"},instructions:"You analyze screenshots/photos of vehicle deal worksheets for Jon Rover Deal Check. Read only numbers and terms actually visible. Never invent missing data. Identify vehicle/MSRP/selling price/discount, rebates, dealer/doc fees, government fees/tax, add-ons/products, trade allowance, payoff/equity, cash down, APR, term, payment, due at signing and total if visible. Explain suspicious or unclear items neutrally; do not accuse a dealer of wrongdoing. A fee being present does not itself make it bad. Distinguish discount from rebates when possible. Return ONLY valid JSON with keys: vehicle, headline, summary, numbers (array of {label,value,note}), flags (array of {title,detail,severity} where severity is info,ask,good), questions (array of strings), confidence (high,medium,low). Keep summary concise and useful. If image is unreadable, use low confidence and explain what needs a clearer screenshot.",input:[{role:"user",content:[{type:"input_text",text:"Analyze this vehicle deal. Give me a plain-English Deal Check and the questions I should ask before signing."},{type:"input_image",image_url:image}]}]})});if(!response.ok){const detail=await response.text();console.error("Deal Check AI error",response.status,detail.slice(0,1000));return NextResponse.json({error:"I couldn't analyze that image. Try a clearer screenshot."},{status:502});}const text=outputText(await response.json()).trim().replace(/^```json\s*/i,"").replace(/```$/,"" ).trim();try{return NextResponse.json(JSON.parse(text));}catch{return NextResponse.json({error:"I could read the image, but couldn't structure the result. Try another screenshot."},{status:502});}}catch(error){console.error(error);return NextResponse.json({error:"Something went wrong analyzing the deal."},{status:500});}}
