import {NextRequest,NextResponse} from 'next/server';
const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;
function headers(){return {'apikey':key!,'Authorization':`Bearer ${key}`,'Content-Type':'application/json'};}
export async function GET(req:NextRequest){if(!url||!key)return NextResponse.json({error:'CRM database not configured'},{status:503});const status=req.nextUrl.searchParams.get('status');const q=req.nextUrl.searchParams.get('q');let endpoint=`${url}/rest/v1/jon_rover_leads?select=*&order=lead_score.desc,last_seen_at.desc&limit=200`;if(status&&status!=='all')endpoint+=`&status=eq.${encodeURIComponent(status)}`;const r=await fetch(endpoint,{headers:headers(),cache:'no-store'});let leads=await r.json();if(q&&Array.isArray(leads)){const s=q.toLowerCase();leads=leads.filter((x:any)=>`${x.name||''} ${x.phone||''} ${x.email||''} ${x.last_request||''} ${(x.desired_models||[]).join(' ')}`.toLowerCase().includes(s));}return NextResponse.json({leads});}
export async function PATCH(req:NextRequest){if(!url||!key)return NextResponse.json({error:'CRM database not configured'},{status:503});const body=await req.json();if(!body.id)return NextResponse.json({error:'Lead id required'},{status:400});const allowed=['name','phone','email','status','lead_score','temperature','summary','next_best_action','next_action_at','notes','assigned_to','budget_min','budget_max','desired_models','desired_exterior','desired_interior','wants_new','wants_used','needs_third_row','trade_in','timeframe'];const patch:Object=Object.fromEntries(Object.entries(body).filter(([k])=>allowed.includes(k)));const r=await fetch(`${url}/rest/v1/jon_rover_leads?id=eq.${encodeURIComponent(body.id)}`,{method:'PATCH',headers:{...headers(),'Prefer':'return=representation'},body:JSON.stringify(patch)});return NextResponse.json({lead:(await r.json())?.[0]});}
