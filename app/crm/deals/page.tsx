"use client";
import {useEffect,useState} from 'react';
type Sheet={name:string;created_at?:string;url:string};
export default function CRMDeals(){const[sheets,setSheets]=useState<Sheet[]>([]);const[error,setError]=useState('');const[selected,setSelected]=useState<Sheet|null>(null);async function load(){const r=await fetch('/api/crm/deal-sheets',{cache:'no-store'});const d=await r.json();if(!r.ok){setError(d.error||'Could not load deal sheets');return}setSheets(d.sheets||[]);setError('');}useEffect(()=>{load()},[]);return <main style={{minHeight:'100vh',background:'#0b100e',color:'#f1eee7',fontFamily:'Arial,Helvetica,sans-serif',padding:'28px'}}><header style={{display:'flex',justifyContent:'space-between',alignItems:'center',borderBottom:'1px solid #303733',paddingBottom:22}}><div><div style={{fontSize:18,letterSpacing:4}}>JON ROVER</div><div style={{fontSize:9,letterSpacing:2.5,color:'#818a85',marginTop:7}}>CRM · DEAL CHECK</div></div><a href="/crm" style={{color:'#f1eee7',textDecoration:'none',fontSize:10,letterSpacing:2}}>← BACK TO CRM</a></header><section style={{maxWidth:1200,margin:'54px auto'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'end',marginBottom:28}}><div><div style={{fontSize:10,letterSpacing:3,color:'#89928d'}}>UPLOADED BY SHOPPERS</div><h1 style={{fontSize:'clamp(42px,7vw,82px)',fontWeight:300,margin:'10px 0 0'}}>Deal Sheets</h1></div><span style={{fontSize:11,color:'#89928d'}}>{sheets.length} SAVED</span></div>{error&&<p>{error}</p>}{!error&&!sheets.length&&<div style={{border:'1px solid #303733',padding:40,color:'#89928d'}}>No Deal Check screenshots have been saved yet. New uploads will appear here automatically.</div>}<div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(250px,1fr))',gap:16}}>{sheets.map(s=><button key={s.name} onClick={()=>setSelected(s)} style={{background:'#111714',border:'1px solid #303733',padding:0,textAlign:'left',color:'#f1eee7',cursor:'pointer'}}>{s.url&&<img src={s.url} alt="Uploaded deal sheet" style={{width:'100%',height:300,objectFit:'cover',objectPosition:'top',display:'block'}}/>}<div style={{padding:18}}><div style={{fontSize:10,letterSpacing:2}}>DEAL CHECK</div><div style={{fontSize:11,color:'#89928d',marginTop:8}}>{s.created_at?new Date(s.created_at).toLocaleString():'Saved deal sheet'}</div></div></button>)}</div></section>{selected&&<div onClick={()=>setSelected(null)} style={{position:'fixed',inset:0,background:'rgba(0,0,0,.88)',zIndex:100,display:'grid',placeItems:'center',padding:24,cursor:'zoom-out'}}><img src={selected.url} alt="Full uploaded deal sheet" style={{maxWidth:'95vw',maxHeight:'92vh',objectFit:'contain'}}/></div>}</main>}
