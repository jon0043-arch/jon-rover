import Link from "next/link";
import "./seo-landing.css";

type Props={eyebrow:string;title:string;lead:string;heading:string;paragraphs:string[];current:string;heroImage?:string;heroAlt?:string};
const pages=[['/range-rover','RANGE ROVER'],['/range-rover-sport','RANGE ROVER SPORT'],['/range-rover-velar','RANGE ROVER VELAR'],['/range-rover-evoque','RANGE ROVER EVOQUE'],['/defender','DEFENDER'],['/discovery-sport','DISCOVERY SPORT'],['/land-rover-certified-pre-owned','CERTIFIED PRE-OWNED'],['/jaguar','JAGUAR']];
export default function ModelLanding({eyebrow,title,lead,heading,paragraphs,current,heroImage,heroAlt}:Props){return <main className="seoPage"><header className="seoNav shell"><Link href="/" className="brandLockup"><span className="brand">JON ROVER</span><span className="eyebrow">JAGUAR LAND ROVER WILLOW GROVE</span></Link><Link className="seoBack" href="/#inventory">VIEW LIVE INVENTORY →</Link></header>{heroImage?<section className="seoVisualHero"><img src={heroImage} alt={heroAlt||current}/><div className="seoVisualShade"/><div className="seoVisualContent shell"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="seoHeroLead">{lead}</p><div className="seoActions"><Link className="button dark" href={`/?search=${encodeURIComponent(current)}#inventory`}>SHOP {current.toUpperCase()} →</Link><Link className="button seoLightButton" href="/#about">WORK WITH JON</Link></div></div></section>:<section className="seoHero shell"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="seoHeroLead">{lead}</p><div className="seoActions"><Link className="button dark" href={`/?search=${encodeURIComponent(current)}#inventory`}>SHOP {current.toUpperCase()} →</Link><Link className="button" href="/#about">WORK WITH JON</Link></div></section>}<section className="seoBody shell"><h2>{heading}</h2><div className="seoBodyText">{paragraphs.map((p,i)=><p key={i}>{p}</p>)}</div></section><section className="seoLinks shell"><p className="eyebrow">EXPLORE JON ROVER</p><div className="seoLinksGrid">{pages.filter(([href])=>href!==locationPath(current)).map(([href,label])=><Link href={href} key={href}>{label}</Link>)}</div></section><footer className="seoFooter shell">JON ROVER · JAGUAR LAND ROVER WILLOW GROVE</footer></main>}
function locationPath(current:string){const x=current.toLowerCase();if(x==='range rover')return '/range-rover';if(x==='range rover sport')return '/range-rover-sport';if(x==='range rover velar')return '/range-rover-velar';if(x==='range rover evoque')return '/range-rover-evoque';if(x==='defender')return '/defender';if(x==='discovery sport')return '/discovery-sport';if(x==='land rover certified pre-owned')return '/land-rover-certified-pre-owned';return '/jaguar'}
