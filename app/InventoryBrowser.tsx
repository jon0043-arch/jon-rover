"use client";

import { useEffect, useMemo, useRef, useState } from "react";

type Vehicle = {
  title: string; condition: string; mileage: number | null; price: number | null; vin: string; url: string;
  image?: string | null; stock?: string | null; exterior?: string | null; interior?: string | null; interiorFamily?: string | null; features?: string[]; pickLabel?: string; why?: string;
};
type Props = { initialQuery?: string; requestSignal?: number };
const JON_TEXT_NUMBER = "+12156087408";
function money(value:number|null){if(value==null)return"Call for price";return new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0}).format(value);}
function miles(value:number|null){if(value==null)return"Mileage unavailable";return `${new Intl.NumberFormat("en-US").format(value)} mi`;}
function interiorLabel(vehicle:Vehicle){if(!vehicle.interior)return null;const family=vehicle.interiorFamily;if(!family||vehicle.interior.toLowerCase().includes(family.toLowerCase()))return vehicle.interior;const friendly=family==="off-white"?"off-white":family==="red-wine"?"red wine":family;return `${vehicle.interior} (${friendly})`;}
function vehicleSms(vehicle:Vehicle){const message=`Hi Jon, I found you through Jon Rover and I'm interested in the ${vehicle.title}${vehicle.stock?` (Stock ${vehicle.stock})`:""}. VIN ${vehicle.vin}. Is it still available?`;return `sms:${JON_TEXT_NUMBER}?body=${encodeURIComponent(message)}`;}
export default function InventoryBrowser({initialQuery="",requestSignal=0}:Props){
 const[query,setQuery]=useState(initialQuery),[vehicles,setVehicles]=useState<Vehicle[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(""),[condition,setCondition]=useState("all"),[total,setTotal]=useState(0),[aiPowered,setAiPowered]=useState(false),[missingAiKey,setMissingAiKey]=useState(false);const requestIdRef=useRef(0),hasMountedRef=useRef(false);
 async function loadInventory(nextQuery=query,nextCondition=condition){const cleaned=nextQuery.trim(),requestId=++requestIdRef.current;setLoading(true);setError("");setAiPowered(false);setMissingAiKey(false);try{const params=new URLSearchParams({limit:cleaned?"15":"12",condition:nextCondition});if(cleaned)params.set("q",cleaned);const response=await fetch(`/api/inventory?${params.toString()}`,{cache:"no-store"});const data=await response.json();if(!response.ok)throw new Error(data?.error||"Could not load inventory");if(requestId!==requestIdRef.current)return;setTotal(data.total||0);const candidates:Vehicle[]=data.vehicles||[];if(!cleaned){setVehicles(candidates);return;}if(!candidates.length){setVehicles([]);return;}const rr=await fetch("/api/recommend",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({query:cleaned,vehicles:candidates})});const rd=await rr.json();if(!rr.ok)throw new Error(rd?.error||"AI recommendation failed");if(requestId!==requestIdRef.current)return;setVehicles((rd.picks||candidates.slice(0,3)).slice(0,3));setAiPowered(Boolean(rd.ai));setMissingAiKey(Boolean(rd.missingKey));}catch(err){if(requestId!==requestIdRef.current)return;setVehicles([]);setError(err instanceof Error?err.message:"Could not load inventory");}finally{if(requestId===requestIdRef.current)setLoading(false);}}
 useEffect(()=>{setQuery(initialQuery);loadInventory(initialQuery,condition);/* eslint-disable-next-line react-hooks/exhaustive-deps */},[requestSignal]);useEffect(()=>{if(!hasMountedRef.current){hasMountedRef.current=true;return;}loadInventory(query,condition);/* eslint-disable-next-line react-hooks/exhaustive-deps */},[condition]);
 const isPickMode=query.trim().length>0;const status=useMemo(()=>{if(loading)return isPickMode?"JON IS CHECKING THE INVENTORY…":"CHECKING WILLOW GROVE NOW…";if(error)return"LIVE INVENTORY TEMPORARILY UNAVAILABLE";if(isPickMode&&aiPowered)return `${vehicles.length} AI-CURATED JON'S PICKS`;if(isPickMode&&missingAiKey)return `${vehicles.length} JON'S PICKS · AI KEY NEEDED`;if(isPickMode)return `${vehicles.length} OF JON'S PICKS`;return `${total||vehicles.length} VEHICLES CONNECTED`;},[loading,error,vehicles.length,isPickMode,total,aiPowered,missingAiKey]);
 return <section id="inventory" className={`inventorySection ${isPickMode?"inventoryPickMode":""}`}><div className="inventoryInner shell"><div className="inventoryTop"><div><p className="eyebrow">{isPickMode?"JON'S 3 PICKS":"LIVE WILLOW GROVE INVENTORY"}</p><h2>{isPickMode?"Three vehicles I'd put at the top of your list.":"Shop the actual inventory without leaving Jon Rover."}</h2>{isPickMode&&aiPowered?<p className="inventoryQueryEcho">I matched your request against the live inventory and narrowed it to these three.</p>:null}</div><span className="inventoryStatus">{status}</span></div><div className="inventoryControls"><form className="inventorySearch" onSubmit={e=>{e.preventDefault();loadInventory(query,condition);}}><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="3 kids, sporty, under $90k, not too huge…" aria-label="Tell Jon what you are looking for"/><button type="submit">{query.trim()?"REFINE MY 3 PICKS →":"SEARCH →"}</button></form><div className="inventoryTabs">{[["all","ALL"],["new","NEW"],["used","PRE-OWNED"]].map(([value,label])=><button key={label} className={condition===value?"active":""} onClick={()=>setCondition(value)} type="button">{label}</button>)}</div></div>{error?<div className="inventoryMessage"><strong>Live inventory couldn't load.</strong><span>{error}</span><button type="button" onClick={()=>loadInventory(query,condition)}>TRY AGAIN →</button></div>:loading?<div className="inventorySkeletonGrid">{Array.from({length:isPickMode?3:6}).map((_,i)=><div className="inventorySkeleton" key={i}/>)}</div>:vehicles.length===0?<div className="inventoryMessage">No strong match yet. Try something like “3 kids, sporty, under $90k, not too huge.”</div>:<div className={`inventoryGrid ${isPickMode?"inventoryPicksGrid":""}`}>{vehicles.map((vehicle,index)=>{const interior=interiorLabel(vehicle);return <article className={`inventoryCard ${isPickMode?"inventoryPickCard":""}`} key={vehicle.vin}><div className="inventoryCardImage">{vehicle.image?<img src={vehicle.image} alt={vehicle.title} loading="lazy"/>:<img src="/hero-defender.png" alt={vehicle.title} loading="lazy"/>}<span>{vehicle.pickLabel||vehicle.condition||"AVAILABLE"}</span>{isPickMode?<div className="inventoryPickBadge">0{index+1}</div>:null}</div><div className="inventoryCardBody"><div className="inventoryCardTopline"><span>{vehicle.exterior||"JLR WILLOW GROVE"}{interior?` · ${interior}`:""}</span>{vehicle.stock?<span>STOCK {vehicle.stock}</span>:null}</div><h3>{vehicle.title}</h3><div className="inventoryFacts"><span>{miles(vehicle.mileage)}</span><span>VIN {vehicle.vin.slice(-6)}</span></div><div className="inventoryPrice">{money(vehicle.price)}</div>{isPickMode?<div className="inventoryWhy"><span>WHY JON PICKED IT</span><p>{vehicle.why||`Pick ${index+1} based on what you told me matters most.`}</p></div>:null}{vehicle.features?.length?<div className="inventoryFeatureRow">{vehicle.features.slice(0,3).map(f=><span key={f}>{f}</span>)}</div>:null}<div className="inventoryActions"><a className="inventoryPrimary" href={vehicleSms(vehicle)}>TEXT JON ABOUT THIS ONE →</a><a className="inventorySecondary" href={vehicle.url} target="_blank" rel="noreferrer">SOURCE SPECS ↗</a></div></div></article>})}</div>}</div></section>;
}
