"use client";

import { useEffect, useMemo, useRef, useState } from "react";
import TextJon from "./TextJon";

type Vehicle = {title:string;condition:string;mileage:number|null;price:number|null;vin:string;url:string;image?:string|null;stock?:string|null;exterior?:string|null;interior?:string|null;interiorFamily?:string|null;features?:string[];pickLabel?:string;why?:string;};
type Props={initialQuery?:string;requestSignal?:number};
const JON_TEXT_NUMBER="+12156087408";
const MODEL_FILTERS=["ALL","DEFENDER","RANGE ROVER","RANGE ROVER SPORT","VELAR","EVOQUE","DISCOVERY","DISCOVERY SPORT","JAGUAR"];
const PICK_LABELS=["BEST MATCH","SMART ALTERNATIVE","WILDCARD"];
const COLOR_WORDS=["black","white","green","blue","red","silver","gray","grey","bronze","gold","yellow","orange","brown","beige","tan","purple"];
function money(value:number|null){if(value==null)return"Call for price";return new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0}).format(value);}
function interiorLabel(v:Vehicle){if(!v.interior)return null;const f=v.interiorFamily;if(!f||v.interior.toLowerCase().includes(f.toLowerCase()))return v.interior;return `${v.interior} (${f==="red-wine"?"red wine":f})`;}
function vehicleSms(v:Vehicle){return `sms:${JON_TEXT_NUMBER}?body=${encodeURIComponent(`Hi Jon, I found you through Jon Rover and I'm interested in the ${v.title}${v.stock?` (Stock ${v.stock})`:""}. VIN ${v.vin}. Is it still available?`)}`;}
function askRover(v:Vehicle){window.dispatchEvent(new CustomEvent("open-jon-rover-vehicle",{detail:{title:v.title,vin:v.vin,price:v.price,exterior:v.exterior,interior:v.interior,stock:v.stock}}));}
function isLeaseSearch(q:string){return /\blease\b|\bleasing\b|\bleased\b/i.test(q);}
function parseBudget(q:string){const m=q.match(/(?:under|below|less than|max(?:imum)?|budget(?: of| is)?|up to|no more than|around|about)\s*\$?\s*([\d,.]+)\s*(k)?/i)||q.match(/\$\s*([\d,.]+)\s*(k)?/i);if(!m)return null;let n=Number(m[1].replace(/,/g,""));if(m[2])n*=1000;return Number.isFinite(n)?n:null;}
function isHardBudget(q:string){return /\b(?:under|below|less than|max(?:imum)?|up to|no more than)\b/i.test(q);}
function requestedColor(q:string){return COLOR_WORDS.find(c=>new RegExp(`\\b${c}\\b`,"i").test(q))||null;}
function colorMatchesVehicle(v:Vehicle,color:string){const exterior=(v.exterior||"").toLowerCase();if(color==="gray"||color==="grey")return exterior.includes("gray")||exterior.includes("grey");return exterior.includes(color);}
function stripMileageWhy(text:string){return text.split(/(?<=[.!?])\s+/).filter(sentence=>!/(?:\bmileage\b|\bmiles?\b|\bodometer\b)/i.test(sentence)).join(" ").trim();}
function specificWhy(v:Vehicle,index:number,q:string){const details:string[]=[];const budget=parseBudget(q);const color=requestedColor(q);if(budget!=null&&v.price!=null){if(v.price<=budget)details.push(`At ${money(v.price)}, it stays inside your ${money(budget)} target.`);else details.push(`At ${money(v.price)}, it is above your ${money(budget)} target and is shown only as an alternative.`);}if(color&&colorMatchesVehicle(v,color))details.push(`It also matches the ${color} exterior you asked for.`);if(!details.length&&v.exterior)details.push(`The ${v.exterior} exterior gives this one a different look from the other choices.`);if(!details.length&&v.price!=null)details.push(`Its ${money(v.price)} asking price is one of the main reasons it made the short list.`);const lead=index===0?`This ${v.title} is the strongest overall match to what you searched for.`:index===1?`I kept this ${v.title} as the second pick because it gives you a meaningful alternative to the first one.`:`This ${v.title} is the wildcard because it gives you a different tradeoff without straying too far from your search.`;return `${lead} ${details.slice(0,2).join(" ")}`.trim();}
function normalizePickReasons(rawPicks:Vehicle[]|undefined,candidates:Vehicle[],q:string){const source=(Array.isArray(rawPicks)&&rawPicks.length?rawPicks:candidates.slice(0,3)).slice(0,3);return source.map((v,index)=>({...v,pickLabel:PICK_LABELS[index],why:stripMileageWhy(String(v.why||""))||specificWhy(v,index,q)}));}
function prioritizeBudget(picks:Vehicle[],candidates:Vehicle[],q:string){const budget=parseBudget(q);if(budget==null)return picks.slice(0,3);const inside=[...picks,...candidates].filter((v,i,a)=>v.price!=null&&v.price<=budget&&a.findIndex(x=>x.vin===v.vin)===i);if(isHardBudget(q)&&inside.length)return inside.slice(0,3);return picks.slice(0,3);}
function normalizeModelParam(model:string){const x=model.trim().toUpperCase();if(x==="RANGE ROVER VELAR")return"VELAR";if(x==="RANGE ROVER EVOQUE")return"EVOQUE";if(x==="LAND ROVER DEFENDER")return"DEFENDER";return MODEL_FILTERS.includes(x)?x:model.trim();}
export default function InventoryBrowser({initialQuery="",requestSignal=0}:Props){const[query,setQuery]=useState(initialQuery),[vehicles,setVehicles]=useState<Vehicle[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(""),[condition,setCondition]=useState("all"),[total,setTotal]=useState(0),[aiPowered,setAiPowered]=useState(false),[browseMode,setBrowseMode]=useState(false),[modelFilter,setModelFilter]=useState("ALL");const requestIdRef=useRef(0),hasMountedRef=useRef(false);
async function loadInventory(nextQuery=query,nextCondition=condition,forceBrowse=false){const cleaned=nextQuery.trim(),requestId=++requestIdRef.current;const browsing=forceBrowse;const effectiveCondition=!browsing&&isLeaseSearch(cleaned)?"new":nextCondition;setLoading(true);setError("");setAiPowered(false);try{const params=new URLSearchParams({limit:browsing?"250":cleaned?"40":"18",condition:effectiveCondition});if(cleaned)params.set("q",cleaned);if(browsing)params.set("browse","1");const response=await fetch(`/api/inventory?${params.toString()}`,{cache:"no-store"});const data=await response.json();if(!response.ok)throw new Error(data?.error||"Could not load inventory");if(requestId!==requestIdRef.current)return;let candidates:Vehicle[]=data.vehicles||[];const color=requestedColor(cleaned);const budget=parseBudget(cleaned);if(!browsing&&color)candidates=candidates.filter(v=>colorMatchesVehicle(v,color));if(!browsing&&isHardBudget(cleaned)&&budget!=null)candidates=candidates.filter(v=>v.price!=null&&v.price<=budget);setTotal(data.total||candidates.length);if(browsing||!cleaned){setVehicles(candidates);return;}if(!candidates.length){setVehicles([]);return;}const rr=await fetch("/api/recommend",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({query:cleaned,vehicles:candidates})});const rd=await rr.json();const raw=rr.ok&&Array.isArray(rd.picks)?rd.picks:[];const budgetOrdered=prioritizeBudget(raw,candidates,cleaned);setVehicles(normalizePickReasons(budgetOrdered,candidates,cleaned));setAiPowered(Boolean(rd.ai));}catch(err){if(requestId!==requestIdRef.current)return;setVehicles([]);setError(err instanceof Error?err.message:"Could not load inventory");}finally{if(requestId===requestIdRef.current)setLoading(false);}}
function runSearch(){setBrowseMode(false);setModelFilter("ALL");loadInventory(query,condition,false);}function showAll(){setBrowseMode(true);setModelFilter("ALL");setQuery("");loadInventory("",condition,true);}function chooseModel(model:string){setBrowseMode(true);setModelFilter(model);const q=model==="ALL"?"":model;setQuery(q);loadInventory(q,condition,true);}useEffect(()=>{const modelParam=typeof window!=="undefined"?new URLSearchParams(window.location.search).get("model"):null;if(modelParam){const model=normalizeModelParam(modelParam);setBrowseMode(true);setModelFilter(model);const q=model==="ALL"?"":model;setQuery(q);loadInventory(q,condition,true);return;}setQuery(initialQuery);setBrowseMode(false);setModelFilter("ALL");loadInventory(initialQuery,condition,false);/* eslint-disable-next-line */},[requestSignal]);useEffect(()=>{if(!hasMountedRef.current){hasMountedRef.current=true;return;}loadInventory(query,condition,browseMode);/* eslint-disable-next-line */},[condition]);
const isPickMode=!browseMode&&query.trim().length>0;const status=useMemo(()=>loading?(isPickMode?"JON IS CHECKING THE INVENTORY…":"CHECKING WILLOW GROVE NOW…"):error?"LIVE INVENTORY TEMPORARILY UNAVAILABLE":isPickMode?`${vehicles.length} ${aiPowered?"AI-CURATED ":""}JON'S PICKS`:browseMode?`${vehicles.length} VEHICLES LOADED`:`${total||vehicles.length} VEHICLES CONNECTED`,[loading,error,isPickMode,vehicles.length,aiPowered,browseMode,total]);
return <section id="inventory" className={`inventorySection ${isPickMode?"inventoryPickMode":""}`}><div className="inventoryInner shell"><div className="inventoryTop"><div><p className="eyebrow">{isPickMode?"JON'S 3 PICKS":"LIVE WILLOW GROVE INVENTORY"}</p><h2>{isPickMode?"Three vehicles I'd put at the top of your list.":"Browse the current inventory."}</h2></div><span className="inventoryStatus">{status}</span></div><div className="inventoryControls"><form className="inventorySearch" onSubmit={e=>{e.preventDefault();runSearch();}}><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Tell Jon what you're looking for…"/><button type="submit">GET JON'S 3 PICKS →</button></form><div className="inventoryTabs">{[["all","ALL"],["new","NEW"],["used","PRE-OWNED"]].map(([value,label])=><button key={label} className={condition===value?"active":""} onClick={()=>setCondition(value)} type="button">{label}</button>)}</div></div><div className="inventoryBrowseBar"><button type="button" className="inventoryBrowseAll" onClick={showAll}>VIEW ALL VEHICLES</button><div className="inventoryModelFilters">{MODEL_FILTERS.map(model=><button type="button" key={model} className={browseMode&&modelFilter===model?"active":""} onClick={()=>chooseModel(model)}>{model}</button>)}</div></div>{error?<div className="inventoryMessage"><strong>Live inventory couldn't load.</strong><span>{error}</span></div>:loading?<div className="inventoryLoader"><div className="inventoryLoaderCopy"><span className="eyebrow">JON ROVER</span><strong>{isPickMode?"Finding your best matches":"Loading live inventory"}</strong></div></div>:vehicles.length===0?<div className="inventoryMessage">No vehicles matched that search.</div>:<div className={`inventoryGrid ${isPickMode?"inventoryPicksGrid":""}`}>{vehicles.map((v,index)=>{const interior=interiorLabel(v);return <article className={`inventoryCard ${isPickMode?"inventoryPickCard":""}`} key={v.vin}><div className="inventoryCardImage">{v.image?<img src={v.image} alt={v.title}/>:<img src="/hero-defender.png" alt={v.title}/>}<span>{v.pickLabel||v.condition||"AVAILABLE"}</span>{isPickMode?<div className="inventoryPickBadge">0{index+1}</div>:null}</div><div className="inventoryCardBody"><div className="inventoryCardTopline"><span>{v.exterior||"EXTERIOR COLOR PENDING"}{interior?` · ${interior}`:""}</span></div><h3>{v.title}</h3><div className="inventoryFacts"><span>VIN {v.vin}</span>{v.stock?<span>STOCK {v.stock}</span>:null}</div><div className="inventoryPrice">{money(v.price)}</div>{isPickMode?<div className="inventoryWhy"><span>WHY JON PICKED IT</span><p>{v.why||specificWhy(v,index,query)}</p></div>:null}<div className="inventoryActions"><button className="inventoryPrimary" type="button" onClick={()=>askRover(v)}>ASK ROVER ABOUT THIS ONE →</button><TextJon className="inventorySecondary" href={vehicleSms(v)}>TEXT JON</TextJon><a className="inventorySecondary" href={v.url} target="_blank" rel="noreferrer">SOURCE SPECS ↗</a></div></div></article>})}</div>}</div></section>;
}
